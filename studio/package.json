{
  "name": "doermann-digital-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "extract": "sanity schema extract --enforce-required-fields --force",
    "typegen": "sanity schema extract --enforce-required-fields --force && sanity typegen generate"
  },
  "dependencies": {
    "@sanity/vision": "^6.9.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "sanity": "^6.9.0",
    "styled-components": "^6.5.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "typescript": "^5"
  }
}
